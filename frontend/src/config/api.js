// API Configuration
// Backend server is running on port 4000
// In production, use relative path to allow Nginx to handle routing
// In development, Vite proxy will forward /api requests to http://localhost:4000
const API_BASE_URL = '';

export default API_BASE_URL;
